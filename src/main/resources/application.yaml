spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: "seuemail@gmail.com"
    password: "ssua senh aqui ookk"
    protocol: smtp
    properties:
      mail:
        smtp:
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

  thymeleaf:
    enable: true

envio:
  email:
    remetente: 'emailremetente@gmail.com'
    nomeRemetente: 'Jorge Luis'

server:
  port: 8082